# Build output & tool artifacts (generated — don't format)
.next/
out/
dist/
coverage/
storybook-static/
playwright-report/
test-results/
node_modules/
*.tsbuildinfo

# Stryker mutation testing — transient sandbox + generated reports (don't format).
# The root .prettierignore has these too, but this package's `format` runs
# `prettier --write .` from this dir, so the closest .prettierignore is what applies.
.stryker-tmp/
stryker.log
reports/mutation.html
reports/mutation/

# Generated source — reproduced verbatim by its generator, never hand-formatted.
# (Mirrors the ESLint global ignores; see CLAUDE.md.)
lib/database.types.ts
*.gen.ts
next-env.d.ts

# Visual-regression baselines — binary PNGs generated by the Storybook test-runner
# (npm run test:storybook:update). Reproduced verbatim, never hand-edited.
__image_snapshots__/
