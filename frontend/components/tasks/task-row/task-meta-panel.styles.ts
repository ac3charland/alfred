/**
 * Visual styling for the meta panel's native date input. `[color-scheme:dark]` themes the
 * browser's built-in date picker to match the dark UI; it's invisible to the component test,
 * so it's extracted here to be locked by a unit test.
 */
export const dateInputClass = '[color-scheme:dark]';
