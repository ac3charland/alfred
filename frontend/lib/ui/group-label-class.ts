/**
 * A result group's label in the shell's search surfaces — the ⌘K palette and the search popover —
 * the smaller, dimmer eyebrow above each group of options, so both lists read as one family.
 */
export const GROUP_LABEL_CLASS =
  'px-2 pb-1 pt-2 text-[10px] font-semibold uppercase tracking-widest text-muted-foreground/70';
