/**
 * A section or group heading: the small uppercase eyebrow that heads the Reader overview's
 * sections and the wiki's index groups, so the two modules' lists read as one family.
 */
export const SECTION_HEADING_CLASS =
  'text-xs font-semibold uppercase tracking-widest text-muted-foreground';
